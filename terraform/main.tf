# Main file retained as compatibility stub.
# Infrastructure is now split across:
# providers.tf, storage.tf, dynamodb.tf, pipeline.tf, lambda.tf,
# appsync_api.tf, appsync_resolvers.tf, scheduler.tf, outputs.tf
